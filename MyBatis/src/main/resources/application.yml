server:
  port: 8899

logging:
  path: logs
  file: mylog.log

spring:
  application:
    name: myTest
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/course
    username: root
    password: 123456

mybatis:
  type-aliases-package:
  mapper-locations:
    - mapper/*